# TIM_state_manuscript
Code to reproduce figures from the TIM/state manuscript.


### Installation
1. Create the environment with `conda create -n state_manuscript python=3.9 numpy cython`
2. Activate the environment with `conda activate state_manuscript`
3. Clone this repository and `cd` into it.
4. Install required Python packages with `pip install -e .`
